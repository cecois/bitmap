var Download = Backbone.Model.extend({
	defaults: {
	},
	idAttribute: "_fakeid",
	rights: function() {
	},
	initialize: function() {
		var series = this.get("series");
		var handle = this.get("handle");
	}

	
});